;;; Purple Dragon
;;;
;;; The Purple Dragon might be a good RoM (Rivers of MUD) based choice
;;; because of friendly immortals and lots of interesting races.
;;;

(emuds-setup-request-eor)
(/trigger)	                        ; reset all triggers
(/mapkey)                               ; reset all keys

(/load "emuds-movement")
